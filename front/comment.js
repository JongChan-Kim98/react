    // Link 리액트에서 a같은 역할을 해줌
    // Link 컴포넌트를 이용해서 경로를 바꿔주고 컴포넌트를 교체해서 보여준다. 라우터간의 이동을 할 수 있게 도와준다.
    // Link에 필요한 props는 to 어디로
    // to이름의 props에 이동할 경로를 넣어준다.

    // useNavigate 똑같이 페이지 이동을 도와주는 아이
    // useNavigate 의 사용방법은 함수 실행 이후 반환받는 객체로 사용을 한다. 

    // 아무거나 써볼게 